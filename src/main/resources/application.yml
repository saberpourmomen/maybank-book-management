server:
  port: 8000
  servlet:
    context-path: /book-management

spring:
  application:
    name: book-management

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=TESTDB;encrypt=false
    username: sa
    password: P@ss123Word
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
external-api:
  book-service:
    base-url: ${BOOK_SERVICE_BASE_URL:http://localhost:8001/external-book/api/v1/book}
    timeout: ${BOOK_SERVICE_TIMEOUT:5}